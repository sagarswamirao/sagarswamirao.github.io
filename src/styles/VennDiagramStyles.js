import { css } from 'styled-components';

const VennDiagramStyles = css`
  #venn-diagram {
    width: 100px;
    height: 100px;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .venn-circle text {
    fill: #fff;
    font-size: 16px;
    font-weight: bold;
  }
`;

export default VennDiagramStyles;
